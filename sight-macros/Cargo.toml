[package]
name = "sight-macros"
version = "0.1.0"
edition = "2021"

[lib]
proc-macro = true

[dependencies]
syn = { version = "2", features = ["full", "parsing"] }
quote = "1.0"
proc-macro2 = "1.0"
static_assertions = "1.1.0"
